ステッパーモータWebコントローラ仕様書

1. プロジェクト概要

PC上の計算モデル（Python/MATLAB）やWebブラウザから，Wi-Fi経由でステッピングモータを直接ドライブするための高精度制御システム．

1.1 解決したい課題と本システムの本質

従来のマイコン制御では，加速曲線を変更するたびにファームウェアの書き換えやビルドが必要でした．本プロジェクトは，**「マイコンを単なるパルス生成エンジン（透過的なブリッジ）」**として扱い，制御ロジックのすべてをPC側（Python等）に持たせることで，開発効率を飛躍的に向上させます．

1.2 何ができるようになるのか

数式をそのまま物理動作へ: Pythonで計算した二次関数，サイン波，S字カーブなどの速度プロファイルを，そのままリアルタイムにモータへ反映できます．

ノンストップ調整: モータを回しながら，PC側のコードを一行書き換えるだけで，次の瞬間の挙動（加速度や目標速度）を変化させられます．

分散処理: 複雑な軌道計算はパワフルなPC（DELL XPS 8940等）で行い，ESP32は通信とパルス出力のみに専念する「役割分担」を実現します．

2. 核心機能

ワイヤレス・ストリーミング制御:

PC側で計算した $v(t)$（速度プロファイル）をUDPパケットでESP32へストリーミングし，モータを即座に追従させます．

L6470 自律パルス生成:

ESP32は速度指令（デジタル値）を送るだけで，実際のパルス生成は専用IC（L6470）が行います．これによりWi-Fi通信負荷によるパルスの乱れを物理的に排除します．

ハイブリッド・インターフェース:

UDP (Port 8888): 高頻度な速度ストリーミング用（リアルタイム制御）．

Webブラウザ (HTTP): パラメータ設定・モニタリング用（UI操作）．

動的マイクロステップ最適化:

速度域に応じて「128分割（低速・静音）」から「フルステップ（高速・高トルク）」までを自動遷移させ，常に最適な駆動状態を維持します．

フェイルセーフ・ウォッチドッグ:

ネットワークトラブル等で指令が途絶えた際，モータを1秒以内に自動停止させ，システムの暴走を防止します．

3. ハードウェア構成

マイコン: ESP32

モータドライバ: L6470 (SPI接続)

配線案 (ESP32 VSPI):

SCK: 18

MISO: 19

MOSI: 23

CS: 5

BUSY: 33

RESET: 32

4. ソフトウェア仕様

4.1 L6470 制御ロジック

電気的パラメータ (固定):

KVAL_HOLD / RUN / ACC / DEC: 0x29 (標準的な12V/24V駆動設定)

OCD (過電流検出): 0x0F (3.375A)

ステップ設定: 速度に応じて 128 $\leftrightarrow$ 32 $\leftrightarrow$ 8 microsteps を自動遷移．

4.2 ネットワーク処理

UDP Listener: ポート8888で待機．受信したJSON {"v": float} を解析．

Watchdog: loop() 内で最終受信時刻を監視し，タイムアウト時に即座に HardStop を実行．

5. 制御プロトコル

5.1 UDP ストリーム

形式: {"v": float} (単位: step/s)

推奨頻度: 20ms ～ 100ms

挙動: 受信により即座に External Mode に移行し，速度を更新．

5.2 mDNS アクセス

ブラウザからは http://motor.local でアクセス可能（IPアドレスの確認不要）．

6. Web UI 仕様

Dashboard: 現在のリアルタイム速度を数値・グラフで可視化．

Control:

Speed Slider: 手動での速度変更．

Mode Switch: Internal(手動) / External(PC連携) の切り替え．

EMG STOP: 最優先のハードウェア停止命令．

更新履歴:

2026/01/07: 機能説明の解像度を高め，名称案を機能重視で再提示．

2026/01/07: 「何を実現するのか」という機能的価値を具体化し，概要・核心機能を再編．

2026/01/07: プロジェクト名称を「EtherSpin-ESP」に決定．