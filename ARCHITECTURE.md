# システムアーキテクチャ

## 概要

EtherSpin-ESPは、ESP32マイコンとL6470ステッピングモータドライバを組み合わせ、Wi-Fi経由でリアルタイム制御を実現するシステムです。

## アーキテクチャ図

```
┌──────────────────────────────────────────────────────────────┐
│                         PC / MATLAB / Python                  │
│  ┌──────────────────┐           ┌──────────────────────┐    │
│  │ Webブラウザ      │           │ 計算モデル           │    │
│  │ (手動制御・監視) │           │ (速度プロファイル)    │    │
│  └────────┬─────────┘           └──────────┬───────────┘    │
└───────────┼────────────────────────────────┼────────────────┘
            │ HTTP/WebSocket                 │ UDP
            │ (Port 80, WS)                  │ (Port 8888)
            │                                │
┌───────────▼────────────────────────────────▼────────────────┐
│                     ESP32 (XIAO ESP32C3)                     │
│  ┌────────────────────────────────────────────────────────┐ │
│  │                   Main Control Loop                     │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │ │
│  │  │ UDP Handler  │  │ Web Server   │  │ Watchdog     │ │ │
│  │  │              │  │              │  │              │ │ │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘ │ │
│  │         │                  │                  │         │ │
│  │         └──────────────────┼──────────────────┘         │ │
│  │                            │                            │ │
│  │         ┌──────────────────▼──────────────────┐         │ │
│  │         │   Speed Control & Step Optimizer    │         │ │
│  │         └──────────────────┬──────────────────┘         │ │
│  └────────────────────────────┼────────────────────────────┘ │
│                                │ SPI                          │
│                     ┌──────────▼──────────┐                  │
│                     │   L6470 Driver IC   │                  │
│                     │  (Pulse Generator)  │                  │
│                     └──────────┬──────────┘                  │
└────────────────────────────────┼─────────────────────────────┘
                                 │ Phase A/B
                      ┌──────────▼──────────┐
                      │  Stepper Motor      │
                      └─────────────────────┘
```

## 主要コンポーネント

### 1. WiFi通信層

#### 役割
- 無線LANアクセスポイントへの接続
- mDNSによる名前解決（motor.local）
- ネットワーク状態の監視

#### 実装
```cpp
void initWiFi() {
  WiFi.mode(WIFI_STA);
  WiFi.begin(WIFI_SSID, WIFI_PASSWORD);
  // 接続待機処理
  MDNS.begin(MDNS_HOSTNAME);
}
```

### 2. UDP通信層

#### 役割
- リアルタイム速度指令の受信
- JSON形式のパケット解析
- ウォッチドッグタイマーの更新

#### プロトコル
```json
// 速度指令パケット
{
  "v": 1500.0  // step/s (正: CW, 負: CCW, 0: 停止)
}
```

#### データフロー
```
PC → UDP:8888 → handleUdpPacket() → JSON Parse → setMotorSpeed()
```

#### 実装の特徴
- ノンブロッキング受信
- パケットロス耐性
- 最小レイテンシ（<10ms）

### 3. Webサーバー層

#### 役割
- ダッシュボードUIの提供
- WebSocketによる双方向通信
- システム状態のブロードキャスト

#### エンドポイント

| エンドポイント | メソッド | 説明 |
|---------------|---------|------|
| `/` | GET | メインダッシュボード |
| `/api/status` | GET | JSON形式のステータス |
| `/ws` | WebSocket | リアルタイム双方向通信 |

#### WebSocketメッセージフォーマット

**クライアント → サーバー**
```json
{
  "cmd": "setSpeed",
  "value": 1000
}
```

**サーバー → クライアント**
```json
{
  "mode": "external",
  "speed": 1000.0,
  "target": 1000.0,
  "running": true,
  "emergency": false,
  "stepMode": 3
}
```

### 4. モータ制御層

#### 動的マイクロステップ最適化

速度域に応じて自動的にステップモードを切り替えます。

```
Speed (step/s)  │  Step Mode  │  特性
─────────────────┼─────────────┼──────────────────
0 - 500         │  1/128      │  低速・静音・高精度
500 - 2000      │  1/32       │  中速・バランス
2000 - 3000     │  1/8        │  高速・高トルク
3000+           │  Full Step  │  最高速・最大トルク
```

#### 制御フロー
```cpp
void setMotorSpeed(float speed) {
  optimizeStepMode(speed);  // ステップモード最適化
  
  if (abs(speed) < 1.0) {
    motor.softStop();       // ソフト停止
  } else {
    unsigned long speedValue = abs(speed);
    if (speed > 0) {
      motor.run(FWD, speedValue);  // 正回転
    } else {
      motor.run(REV, speedValue);  // 逆回転
    }
  }
}
```

### 5. L6470ドライバ層

#### 初期化シーケンス
```
1. SPIバス初期化 (SCK, MISO, MOSI, CS)
2. RESET信号によるハードウェアリセット
3. 電気的パラメータ設定
   - KVAL (トルク設定)
   - OCD (過電流検出)
   - ACC/DEC (加速度)
4. ステップモード設定
```

#### SPI通信プロトコル
- クロック周波数: 4MHz
- モード: SPI_MODE3
- ビットオーダー: MSB First

#### レジスタ設定

| レジスタ | 値 | 説明 |
|---------|-----|------|
| KVAL_HOLD | 0x29 | ホールディングトルク |
| KVAL_RUN | 0x29 | 運転時トルク |
| KVAL_ACC | 0x29 | 加速時トルク |
| KVAL_DEC | 0x29 | 減速時トルク |
| OCD_TH | 0x0F | 過電流検出: 3.375A |
| STEP_MODE | 0-7 | マイクロステップ設定 |

### 6. ウォッチドッグ層

#### 役割
- 通信途絶検出
- 自動停止による安全確保
- モード自動復帰

#### 動作ロジック
```cpp
void checkWatchdog() {
  if (state.mode == MODE_EXTERNAL) {
    unsigned long elapsed = millis() - state.lastUdpTime;
    
    if (elapsed > WATCHDOG_TIMEOUT_MS && state.motorRunning) {
      motor.softStop();
      state.motorRunning = false;
      state.mode = MODE_INTERNAL;  // 安全なモードに復帰
    }
  }
}
```

#### タイムアウト設定
- デフォルト: 1000ms
- 推奨範囲: 500ms - 2000ms
- 調整方法: `WATCHDOG_TIMEOUT_MS` 定数を変更

## 制御モード

### Internal Mode（手動制御）
- Webブラウザからの手動操作
- スライダーによる速度設定
- ウォッチドッグ無効

### External Mode（PC連携）
- UDP経由の自動制御
- 継続的な速度更新が必要
- ウォッチドッグ有効（安全機能）

### モード遷移
```
Internal ←→ External
    ↑           ↓
    │  UDP受信時に自動遷移
    │  タイムアウト時に自動復帰
    └───────────┘
```

## メモリ使用量

### プログラム領域
- Flash使用量: 約900KB / 4MB
- 余裕: 十分な空き領域

### 動的メモリ
- RAM使用量: 約40KB / 400KB
- スタック: 約8KB
- ヒープ: 約32KB（主にWiFi/WebSocketバッファ）

### 最適化ポイント
- HTML文字列はFlashに配置（PROGMEM）
- JSON処理は静的バッファを使用
- WebSocketクライアント数制限（最大4接続）

## レイテンシ分析

### UDP → モータ応答時間

```
PC送信 → ESP32受信 → JSON解析 → 速度設定 → SPI通信 → L6470応答
  1ms      2ms         1ms        1ms        2ms       3ms
                                                    
合計: 約10ms（理論値）
実測: 15-25ms（WiFi遅延含む）
```

### 改善手法
1. UDP送信バッファサイズ増加
2. WiFi QoS設定
3. JSON解析の最適化
4. SPI速度の向上（現在4MHz、最大10MHz可能）

## エラーハンドリング

### WiFi切断時
```cpp
// 自動再接続機能（実装推奨）
void checkWiFiConnection() {
  if (WiFi.status() != WL_CONNECTED) {
    WiFi.reconnect();
  }
}
```

### L6470エラー検出
```cpp
// ステータスレジスタ監視
uint16_t status = motor.getStatus();
if (status & STATUS_UVLO) {
  // 低電圧検出
}
if (status & STATUS_OCD) {
  // 過電流検出
}
```

### 緊急停止
```cpp
void emergencyStop() {
  motor.hardStop();        // 即座に停止
  state.emergencyStop = true;
  // リセットボタンで復帰可能
}
```

## パフォーマンス特性

### 速度範囲
- 最小: 1 step/s
- 最大: 15625 step/s (L6470の仕様上限)
- 推奨範囲: 10 - 5000 step/s

### 加速度
- デフォルト: 0x0A0（中程度）
- 調整可能範囲: 0x001 - 0xFFF
- 高速アプリケーション: 0x200以上推奨

### 精度
- 位置精度: マイクロステップ依存（最高1/128ステップ）
- 速度精度: ±0.01 step/s
- タイミング精度: ±1ms

## 拡張性

### 複数モータ対応
現在のアーキテクチャでは、CSピンを増やすことで複数L6470の制御が可能：

```cpp
AutoDriver motor1(PIN_CS1, PIN_RESET);
AutoDriver motor2(PIN_CS2, PIN_RESET);
AutoDriver motor3(PIN_CS3, PIN_RESET);
```

### センサー統合
- エンコーダー入力対応（GPIO追加）
- リミットスイッチ対応（割り込み使用）
- 温度センサー統合

### 通信プロトコル拡張
- MQTT対応
- CoAP対応
- REST API拡張

## セキュリティ考慮事項

### 現在の制限
- 認証機能なし
- 暗号化なし
- 同一ネットワーク上からのアクセス制限なし

### 改善案
1. WPA2-Enterprise対応
2. HTTPS/WSS対応
3. APIキー認証
4. アクセス制御リスト（ACL）

## テスト戦略

### 単体テスト
- SPI通信テスト
- JSON解析テスト
- ウォッチドッグ動作テスト

### 統合テスト
- UDP → モータ動作の一貫性
- WebSocket双方向通信
- モード遷移テスト

### 負荷テスト
- 高頻度UDP送信（100Hz）
- 複数WebSocketクライアント
- 長時間連続動作（24時間以上）

## 既知の制限事項

1. **WiFi範囲**: ESP32の通信範囲に依存（約10-50m）
2. **同時接続**: WebSocketクライアントは4接続まで
3. **レイテンシ**: WiFi環境により15-50ms
4. **精度**: 機械的振動、負荷変動の影響を受ける

## 今後の改善計画

- [ ] 位置制御モードの追加
- [ ] エンコーダーフィードバック対応
- [ ] OTA（無線アップデート）対応
- [ ] データロギング機能
- [ ] グラフィカルプロッティング機能
- [ ] スマートフォンアプリ対応

---

詳細な実装については、ソースコード内のコメントを参照してください。
